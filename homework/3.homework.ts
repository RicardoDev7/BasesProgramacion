(() => {
    /**
     *  Calcule el área de un triangulo rectángulo que Base 10 y altura 5
     */
    let base = 10;
    let height = 5;
    let area = (base * height)/2;
    console.log(`El área del triangulo rectángulo de base ${base} y altura ${height} es ${area} metros`);
})();